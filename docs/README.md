# Documentation — ISSF Pistol Sighting Simulator

*Placeholder.* The written documentation (theory, equations, exercises) is to be
added here, mirroring the structure of the rifle simulator's `docs/`:

- a LaTeX source (`doc.tex`) compiled to `doc.pdf`;
- a chapter-per-file Markdown version under `docs/md/` plus SVG figures,
  generated from the LaTeX source with `tex2html.py`.

Planned chapters (pistol-specific):

1. Introduction & user manual
2. Sight-alignment (angular) error vs. point-of-aim (parallel) error
3. Arm sway / wobble model
4. Visual accommodation (focusing on the front sight)
5. Hold: 6-o'clock vs. centre
6. Mechanical sight adjustment (clicks)
7. Wind drift and cant
8. Scoring model (ISSF 10 m / 25 m pistol targets)
9. Implementation notes
